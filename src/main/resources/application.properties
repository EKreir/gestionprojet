spring.application.name=gestionprojet
# Configuration MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/gestionprojet?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=eliess
spring.datasource.password=Eliess2001\#@!

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# JWT (dev only - en prod utilisez une variable d'environnement)
#app.jwt.secret=change_this_is_dev_use_a_long_random_string_at_least_32_bytes
#app.jwt.expiration-ms=3600000

#Important / conseils s